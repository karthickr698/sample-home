// import { selectCourseDetailsDomain } from '../selectors';

describe('selectCourseDetailsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
