/*
 * CourseDetails Messages
 *
 * This contains all the text for the CourseDetails container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CourseDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CourseDetails container!',
  },
});
