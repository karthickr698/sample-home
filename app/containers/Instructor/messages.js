/*
 * Instructor Messages
 *
 * This contains all the text for the Instructor container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Instructor';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Instructor container!',
  },
});
