/*
 * AllCourse Messages
 *
 * This contains all the text for the AllCourse container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AllCourse';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AllCourse container!',
  },
});
